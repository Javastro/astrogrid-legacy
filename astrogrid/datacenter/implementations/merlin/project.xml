<?xml version="1.0" encoding="UTF-8"?>
<project>
  <extend>${basedir}/../../project-base.xml</extend>
  <!-- the version of maven's project object model -->
  <pomVersion>3</pomVersion>

  <!-- a unique name for this project -->
  <id>merlin</id>

  <!-- a short but descriptive name for the project -->
  <name>Merlin Datacenter</name>

  <!-- The version of the project under development, e.g.
       1.1, 1.2, 2.0-dev -->
  <currentVersion>HEAD</currentVersion>


  <description>
	Merlin Datacenter
  </description>
  <!-- a short description of what the project does -->
  <shortDescription>
    Merlin Datacenter
  </shortDescription>

  <siteDirectory>/var/www/www/maven/build/datacenter/implementations/merlin</siteDirectory>

  <!-- the version control repository and http url for online access
       the connection element has the form:
       scm:<system>:<system specific connection string> -->
  <repository>
    <connection>
      scm:cvs:pserver:anoncvs@cvs.astrogrid.org:/devel:astrogrid/datacenter/implementations/merlin
    </connection>
    <developerConnection>
      scm:cvs:ext:${maven.username}@cvs.astrogrid.org:/devel:astrogrid/datacenter/implementations/merlin
    </developerConnection>
  </repository>


  <!-- jar files the project is dependent on -->
  <dependencies>

	<dependency>
		<groupId>astrogrid-datacenter</groupId>
		<artifactId>astrogrid-datacenter-server</artifactId>
		<version>SNAPSHOT</version>
	</dependency>

  </dependencies>

</project>

