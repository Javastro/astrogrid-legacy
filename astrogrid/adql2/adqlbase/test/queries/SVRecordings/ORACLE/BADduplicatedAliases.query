+=====================================================+
|  Input ADQL:                                        |
+=====================================================+
select 
	min(a.ra) as minra, 
	max(a.ra) as maxra, 
	min(a."dec") as minra, 
	max(a."dec") as maxra  
from cata as a
where a.signal >= 23 ;
+=====================================================+
|  Output SQL:                                        |
+=====================================================+
Select MIN( a.ra)  minra, MAX( a.ra)  maxra, MIN( a.dec)  minra, MAX( a.dec)  maxra From cata a  Where a.signal >= 23 