<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" >

    <extend>${basedir}/../../maven-base/project.xml</extend>
        <artifactId>workbench-userdocs-javahelp</artifactId>
        <currentVersion>${astrogrid.desktop.version}</currentVersion>
        <name>Workbench User Documentation</name>
        <description>
Helpfiles, etc for workbench.
	</description>
        <shortDescription>Workbench User Documentation.</shortDescription>
 
        <build>
                <sourceDirectory/>
                <resources>
                        <resource>
                                <directory>${basedir}/src</directory>
                        </resource>
                        <resource>
								<directory>${basedir}/target/index</directory>
								<includes>
									<include>**/*</include>
								</includes>
						</resource>                   
                </resources>
        </build>

        <dependencies>
         <dependency>
         <!-- used for indexing -->
	 <artifactId>jhall</artifactId>
	 <groupId>javahelp</groupId>
	 <version>2.0.02</version>
	 <jar>jhall.jar</jar>
 	</dependency>
        </dependencies>

</project>
