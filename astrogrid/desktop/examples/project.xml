<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" >

    <extend>${basedir}/../../maven-base/project.xml</extend>
        <artifactId>ar-script-examples</artifactId>
        <currentVersion>${astrogrid.desktop.version}</currentVersion>
        <name>AR Scripts</name>
        <description>
	Scripts: both examples and utilities: that access VO through the Astro Runtime
	</description>
        <shortDescription>Astro Runtime Scripts</shortDescription>
 
        <build>
                <sourceDirectory/>
                <resources>
                        <resource>
                                <directory>${basedir}/src</directory>
                        </resource>
                   
                </resources>
        </build>

        <dependencies/>

</project>
