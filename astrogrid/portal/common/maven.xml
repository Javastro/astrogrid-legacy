<project
    xmlns:j="jelly:core"
    xmlns:u="jelly:util"
    xmlns:ant="jelly:ant">

  <!-- Conditionally disable linkcheck based on a property. --> 
<!--
  <postGoal name="war:webapp">  
    <j:set var="webapp.dir" value="${pom.getPluginContext('maven-war-plugin').getVariable('maven.war.webapp.dir')}"/>

    <ant:delete dir="${webapp.dir}/WEB-INF/classes"/>
  </postGoal> 
-->
</project>
