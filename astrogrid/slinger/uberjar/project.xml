<project>
    <extend>${basedir}/../api/project.xml</extend>
    <artifactId>astrogrid-slinger</artifactId>
    <name>Astrogrid Slinger UberJar</name>
    <description>This project takes the Slinger API and repackages it as a single jar</description>
    <shortDescription>This project takes the Slinger API and repackages it as a single jar</shortDescription>
    <dependencies>

        <!-- need common for the old User and Ivorns -->
        <dependency>
            <artifactId>astrogrid-slinger-api</artifactId>
            <groupId>org.astrogrid</groupId>
            <version>${astrogrid.slinger.version}</version>
        </dependency>
        <dependency>
            <groupId>maven-plugins</groupId>
            <artifactId>maven-javaapp-plugin</artifactId>
            <version>1.3</version>
            <type>plugin</type>
        </dependency> 
    </dependencies>
</project>
