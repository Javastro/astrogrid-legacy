<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>astrogrid-workflow-objects</artifactId>
  <version>2007.2wo</version>
  <name>Astrogrid Workflow Common Objects</name>
     <parent>
      <artifactId>astrogrid-base</artifactId>
      <groupId>org.astrogrid</groupId>
      <version>2009.1-SNAPSHOT</version>
    </parent>
  <description>
            This project contains classes -- Java beans -- generated by tools 
            from XSD schemata and WSDL. The beans are used by other projects
            to marshall and unmarshall messages between services.
             
            Many of these classes are contractual: they form part of the
            published APIs to delegate libraries for AstroGrid services.
            Therefore, the classes should not be changed casually.
             
            The classes are stored and maintained in CVS in the same way as
            normal, hand-written code. The classes can be regenerated from
            their schemata (which are stored in the contracts project) 
            by a Maven goal in this
            project, but they must then be merged by hand into the source tree.
            This approach allows one to regenerate all the classes and then to
            retain only selected updates.
            </description>
            
                <scm>
      <connection>scm:cvs:pserver:anonymous:@cvs2.astrogrid.org:/astrogrid:devel/astrogrid/workflow-objects/</connection>
      <developerConnection>scm:cvs:ext:${maven.username}@cvs.astrogrid.org:/devel:astrogrid/workflow-objects/</developerConnection>
      <url>http://www.astrogrid.org/viewcvs/astrogrid/workflow-objects/</url>
   </scm>
            
  <build>
    <sourceDirectory>src/java</sourceDirectory>
    <resources>
      <resource>
        <directory>${basedir}/src/java</directory>
        <includes>
          <include>**/*.xml</include>
          <include>**/*.properties</include>
        </includes>
      </resource>
      <resource>
        <directory>${basedir}</directory>
        <includes>
          <include>wsdd/*.wsdd</include>
        </includes>
      </resource>
    </resources>
     
    <plugins>
            <plugin>
            <artifactId>maven-compiler-plugin</artifactId>
            <configuration>
               <showDeprecation>true</showDeprecation>
               <source>1.4</source>
               <target>1.4</target>
               <verbose>false</verbose>
            </configuration>
         </plugin>
 
       <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>-ea</argLine>
          <testFailureIgnore>true</testFailureIgnore>
        </configuration>
      </plugin>
   </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.astrogrid</groupId>
      <artifactId>astrogrid-common</artifactId>
      <version>2007.2.01</version>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>3.8</version>
    </dependency>
    <dependency>
      <groupId>xmlunit</groupId>
      <artifactId>xmlunit</artifactId>
      <version>1.0</version>
    </dependency>
    <dependency>
      <groupId>commons-jxpath</groupId>
      <artifactId>commons-jxpath</artifactId>
      <version>1.1</version>
    </dependency>
    <dependency>
      <groupId>castor</groupId>
      <artifactId>castor</artifactId>
      <version>0.9.5</version>
    </dependency>
    <dependency>
      <groupId>xerces</groupId>
      <artifactId>xerces</artifactId>
      <version>2.4.0</version>
    </dependency>
    <dependency>
      <groupId>xml-apis</groupId>
      <artifactId>xml-apis</artifactId>
      <version>1.0.b2</version>
    </dependency>
    <dependency>
      <groupId>axis</groupId>
      <artifactId>axis</artifactId>
      <version>1.3</version>
    </dependency>
  </dependencies>
  <reporting>
    <plugins>
         <plugin>
        <artifactId>maven-javadoc-plugin</artifactId>
        <configuration>
          <source>1.4</source>
          <tags>
            <tag>
              <head>To Do:</head>
              <name>todo</name>
              <placement>a</placement>
            </tag>
            <tag>
              <head>Future:</head>
              <name>future</name>
            </tag>
            <tag>
              <head>Modified:</head>
              <name>modified</name>
              <placement>a</placement>
            </tag>
          </tags>
        </configuration>
      </plugin>
    
    </plugins>
  </reporting>
 <url>${astrogrid_doc_URL_root}/p/workflow-objects/${project.version}</url> 
 <distributionManagement>
    <site>
      <id>agsite</id>   <!-- this is the desired layout of the site and - will have to be repeated in the  projects unfortunately until fixes to http://jira.codehaus.org/browse/MNG-2915 are available  -->
 
      <name>Default Site</name>
      <url>${astrogrid_doc_server_root}/p/workflow-objects/${project.version}</url>
      <!-- terror.star.le.ac.uk/ -->
    </site>
    
 </distributionManagement>
</project>
