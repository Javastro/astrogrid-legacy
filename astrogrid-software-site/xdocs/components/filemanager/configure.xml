<?xml version="1.0" encoding="ISO-8859-1"?>
<document>
	<properties>
		<author email="astro.maven.docs@codon.demon.co.uk">Dave Morris</author>
		<title>Configuration</title>
	</properties>
	<body>
		<section name="Configuration">
			<p>
				The FileManager service needs the following configuration properties:
				<ul>
					<li>The configuration name for this service.</li>
					<li>The service identifier for this service.</li>
					<li>The filesystem directory to store the metadata files.</li>
					<li>The service identifier of the default <a href="../filestore/index.html">FileStore</a>.</li>
					<li>The service endpoint of a <a href="../registry/index.html">Registry</a> service.</li>
				</ul>

				Example :
				<table>
					<tr>
						<td>Config property</td>
						<td>Example Value</td>
					</tr>
					<tr>
						<td>org.astrogrid.filemanager.service.name</td>
						<td>org.astrogrid.filemanager</td>
					</tr>
					<tr>
						<td>org.astrogrid.filemanager.service.ivorn</td>
						<td>ivo://org.astrogrid.example/filemanager</td>
					</tr>
					<tr>
						<td>org.astrogrid.filemanager.filestore.ivorn</td>
						<td>ivo://org.astrogrid.example/filestore</td>
					</tr>
					<tr>
						<td>org.astrogrid.filemanager.basedir</td>
						<td>/var/astrogrid/filemanager/data</td>
					</tr>
					<tr>
						<td>org.astrogrid.registry.query.endpoint</td>
						<td>http://example.astrogrid.org:8080/registry/services/RegistryQuery</td>
					</tr>
				</table>
			</p>
			<subsection name="Service name">
				<p>
					This property is used to set the name prefix used to load the rest of the configuration properties.
					<br/>
					This is primarily used for test environments. In a live system, this should be set to <code>org.astrogrid.filemanager</code>.
				</p>
			</subsection>
			<subsection name="Service ivorn">
				<p>
					In order to generate identifiers for the metadata nodes, FileManager needs to know the the registry identifier for this service.
					<br/>
					This should be set to the full <code>ivo://authority/resource</code> identifier for this service. 
				</p>
			</subsection>
			<subsection name="FileStore ivorn">
				<p>
					The FileManager service needs to know the identifier of at least one <a href="../filestore/index.html">FileStore</a> service.
					This will be used as the default if a client request does not specify a specific location.
					<br/>
					This should be set to the full <code>ivo://authority/resource</code> identifier of the <a href="../filestore/index.html">FileStore</a> service. 
				</p>
			</subsection>
			<subsection name="Base directory">
				<p>
					The FileManager service needs access to a directory in the local filesystem to store its data.
					<br/>
					The FileManager service should have read/write access to the directory.
				</p>
			</subsection>
			<subsection name="Registry endpoint">
				<p>
					In order to be able to resolve references to other AstroGrid services, FileManager needs to know the
					endpoint address of at least one <a href="../registry/index.html">Registry</a> service.
					<br/>
					This should be set to the full <code>http://....</code> endpoint URL of the <a href="../registry/index.html">Registry</a> service. 
				</p>
			</subsection>
		</section>
	</body>
</document>
