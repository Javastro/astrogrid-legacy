/* Generated By: AdqlStoX.jjt,v 1.11 2007/07/16 21&JJTree: Do not edit this line. AST_Point.java */

package org.astrogrid.adql;

import org.apache.xmlbeans.XmlObject;
import org.astrogrid.adql.beans.PointType;
import org.astrogrid.adql.beans.ScalarExpressionType;
import org.astrogrid.stc.beans.*;
import org.apache.commons.logging.Log ;
import org.apache.commons.logging.LogFactory ;

public class AST_Point extends SimpleNode {
  
    private static Log log = LogFactory.getLog( AST_Point.class ) ;
    
  public AST_Point(AdqlStoX p, int id) {
    super(p, id);
  }

  public void buildXmlTree( XmlObject xo ) {
      if( log.isTraceEnabled() ) enterTrace( log, "AST_Point.buildXmlTree()" ) ; 
      PointType pt = (PointType)xo.changeType( PointType.type ) ;
      int childCount = jjtGetNumChildren() ;
      for( int i=0; i<childCount; i++ ) {
          children[i].buildXmlTree( pt.addNewCoord() ) ;
      }   
      this.generatedObject = pt ;
      super.buildXmlTree(pt) ;
      if( log.isTraceEnabled() ) exitTrace( log, "AST_Point.buildXmlTree()" ) ; 
  }
}
