/* Generated By: AdqlStoX.jjt,v 1.18 2007/07/25 12&JJTree: Do not edit this line. AST_ConvexCartesian.java */

package org.astrogrid.adql;

import org.apache.xmlbeans.XmlObject;
import org.astrogrid.stc.beans.ConvexType;
import org.apache.commons.logging.Log ;
import org.apache.commons.logging.LogFactory ;

public class AST_ConvexCartesian extends SimpleNode {
    
  private static Log log = LogFactory.getLog( AST_ConvexCartesian.class ) ;

  public AST_ConvexCartesian(AdqlStoX p, int id) {
    super(p, id) ;
  } 
  
  public void buildXmlTree( XmlObject xo ) {
      if( log.isTraceEnabled() ) enterTrace( log, "AST_ConvexCartesian.buildXmlTree()" ) ; 
      getTracker().setType( ConvexType.type ) ;
      //
      // We know this is a Cartesian Convex type, else why are we here.
      // So set the appropriate astro coord system...
      AST_RegionPredicate.setAstroCoordSystem_Cartesian( this ) ;
      //
      // "Create" the appropriate convex type using XmlBeans magic.
      ConvexType p = (ConvexType)xo.changeType( ConvexType.type ) ;
    
      this.generatedObject = p ;
      super.buildXmlTree(p) ;
      if( log.isTraceEnabled() ) exitTrace( log, "AST_ConvexCartesian.buildXmlTree()" ) ; 
  }

}

